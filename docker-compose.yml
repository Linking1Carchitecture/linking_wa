version: '3.7'

services:
  linking_wa:
    container_name: linking_wa
    build: .
    volumes:
      - './:/linking_wa'
      - '/linking_wa/node_modules'
    ports:
      - 3000:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
      - FAST_REFRESH=false
    stdin_open: true